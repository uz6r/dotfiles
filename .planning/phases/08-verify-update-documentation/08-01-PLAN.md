---
phase: 08-verify-update-documentation
plan: 08-01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .planning/codebase/STRUCTURE.md
autonomous: true
requirements:
  - DOC-01
user_setup: []

must_haves:
  truths:
    - "User can read README.md without any Courtsite/sinar/enjin references"
    - "User can see correct git clone URL (`uz6r` not `yourname`) in README.md bootstrap instructions"
    - "User can see README.md directory tree listing current bin/ contents (audit-nvim-plugins, profile-zsh) instead of deleted sinar scripts"
    - "User can read STRUCTURE.md with bin/ directory tree showing current scripts, no sinar entries, no scripts/ entry"
  artifacts:
    - path: "README.md"
      provides: "Clean documentation with current bin/ contents and correct repo URL"
      min_lines: 128
    - path: ".planning/codebase/STRUCTURE.md"
      provides: "Updated directory tree reflecting post-cleanup state"
      min_lines: 70
  key_links:
    - from: "README.md"
      to: "git@github.com:uz6r/dotfiles.git"
      pattern: "git@github.com:uz6r/dotfiles\\.git"
    - from: "README.md directory tree"
      to: "bin/ contents"
      pattern: "audit-nvim-plugins"
    - from: "STRUCTURE.md directory tree"
      to: "current bin/ contents"
      pattern: "audit-nvim-plugins"
---

<objective>
Update README.md and STRUCTURE.md to reflect post-cleanup state after Phase 6 and Phase 7.

Purpose: Remove all Courtsite/sinar/enjin references from user-facing documentation. Fix stale git clone URL. Update directory trees to show actual current bin/ contents (audit-nvim-plugins, profile-zsh) instead of deleted sinar scripts.

Output: Clean README.md with no Courtsite references and correct repo URL. Updated STRUCTURE.md with current bin/ contents and no scripts/ entry.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-verify-update-documentation/08-CONTEXT.md
@.planning/phases/07-remove-company-scripts/07-01-SUMMARY.md
@.planning/codebase/STRUCTURE.md
</context>

<interfaces>
<!-- Current bin/ contents (post Phase 7 cleanup) -->
From `ls bin/`:
```
audit-nvim-plugins   # Generic Neovim plugin audit script
profile-zsh          # Generic Zsh profiling script
```

No scripts/ directory contents remain (empty after Phase 6 update-migration-pr deletion).
</interfaces>

<tasks>

<task type="auto">
  <name>task 1: Full README refresh — remove Courtsite refs, fix URL, update directory tree</name>
  <files>README.md</files>
  <read_first>README.md</read_first>
  <acceptance_criteria>
    - `grep -n "sinar\|courtsite\|enjin\|Courtsite\|yourname\|TODO" README.md` must return no matches
    - `grep -n "git@github.com:uz6r/dotfiles" README.md` must return a match (line ~68)
    - `grep -n "audit-nvim-plugins\|profile-zsh" README.md` must return matches in the directory tree section
    - `grep -c "\.zshrc" README.md` must still return > 0 (other zshrc references preserved)
    - `grep -n "powerlevel10k" README.md` must still return a match (other content preserved)
  </acceptance_criteria>
  <action>
    Per D-01, D-02, D-03, D-04, D-05. Make these exact changes to README.md:

    **1. Fix git clone URL (line 68, per D-04):**
    Change:
    ```sh
    git clone git@github.com:yourname/dotfiles.git ~/dotfiles
    ```
    To:
    ```sh
    git clone git@github.com:uz6r/dotfiles.git ~/dotfiles
    ```

    **2. Update directory tree bin/ entries (lines 96-98, per D-02, D-05):**
    Replace:
    ```
    ├── bin/             # personal scripts (symlinked into ~/bin)
    │   ├── sinar-pi-setup       # build + package sinarclient tarball
    │   └── sinar-pi-wifi-setup  # wifi setup for Pi (nmconnection + rsync)
    ```
    With:
    ```
    ├── bin/             # personal scripts (symlinked into ~/bin)
    │   ├── audit-nvim-plugins   # audit neovim plugin state
    │   └── profile-zsh          # profile zsh startup time
    ```

    **3. Remove Courtsite-related aliases note (line 132, per D-03):**
    Find the notes section and remove the line:
    `- Some Courtsite-related aliases are being phased out — see `.zshrc` comments marked `TODO``
    
    This line appears at line 132 in the notes section. Remove just this line, preserving all other notes bullet points.

    **4. Verify no other stale references:** After the above changes, run:
    `rg -i "sinar|courtsite|enjin|yourname" README.md`
    — must return no matches.
  </action>
  <verify>
    <automated>
      ! rg -i "sinar|courtsite|enjin|Courtsite|yourname" README.md && \
      rg -q "git@github.com:uz6r/dotfiles" README.md && \
      rg -q "audit-nvim-plugins" README.md && \
      rg -q "profile-zsh" README.md && \
      echo "✅ README updated"
    </automated>
  </verify>
  <done>README.md has zero Courtsite/sinar/enjin references, correct git clone URL (uz6r), directory tree shows audit-nvim-plugins and profile-zsh instead of sinar scripts, and the "Courtsite-related aliases" note is removed.</done>
</task>

<task type="auto">
  <name>task 2: Update STRUCTURE.md directory tree</name>
  <files>.planning/codebase/STRUCTURE.md</files>
  <read_first>.planning/codebase/STRUCTURE.md</read_first>
  <acceptance_criteria>
    - `grep -n "sinar-pi-setup\|sinar-pi-wifi-setup\|update-migration-pr" .planning/codebase/STRUCTURE.md` must return no matches
    - `grep -n "audit-nvim-plugins" .planning/codebase/STRUCTURE.md` must return a match
    - `grep -n "profile-zsh" .planning/codebase/STRUCTURE.md` must return a match
    - `grep -n "scripts/" .planning/codebase/STRUCTURE.md` must return no matches (scripts/ entry removed)
  </acceptance_criteria>
  <action>
    Per D-08. Make these exact changes to `.planning/codebase/STRUCTURE.md`:

    **1. Replace bin/ entries (lines 41-43):**
    Change from:
    ```
    ├── bin/                 # → ~/bin/
    │   ├── sinar-pi-setup       # Build sinarclient tarball
    │   └── sinar-pi-wifi-setup  # Pi wifi setup
    ```
    To:
    ```
    ├── bin/                 # → ~/bin/
    │   ├── audit-nvim-plugins   # Audit neovim plugin state
    │   └── profile-zsh          # Profile zsh startup time
    ```

    **2. Remove scripts/ entry (lines 46-47):**
    Find and remove these two lines:
    ```
    └── scripts/             # Utility scripts (not symlinked)
        └── update-migration-pr
    ```

    The scripts/ directory is empty after Phase 6 deleted update-migration-pr and Phase 7 removed the PATH reference. The entry no longer represents meaningful content.

    **3. Update Naming Conventions section if needed:**
    Check line ~65 (Naming Conventions section). The `sinar-pi-setup` example 
    was used as a script naming convention example. If found, it should be 
    replaced with a current script example like `audit-nvim-plugins`. Run:
    `grep -n "sinar-pi-setup\|sinar-pi-wifi-setup\|update-migration-pr" .planning/codebase/STRUCTURE.md`
    to check. If any matches found, replace them with `audit-nvim-plugins` as 
    the naming example.
  </action>
  <verify>
    <automated>
      ! rg -q "sinar-pi-setup|sinar-pi-wifi-setup|update-migration-pr" .planning/codebase/STRUCTURE.md && \
      rg -q "audit-nvim-plugins" .planning/codebase/STRUCTURE.md && \
      rg -q "profile-zsh" .planning/codebase/STRUCTURE.md && \
      ! rg -q "scripts/" .planning/codebase/STRUCTURE.md && \
      echo "✅ STRUCTURE.md updated"
    </automated>
  </verify>
  <done>STRUCTURE.md directory tree shows current bin/ scripts (audit-nvim-plugins, profile-zsh), no sinar entries, no scripts/ entry. Script naming conventions example updated if needed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only changes. No network access, data handling, authentication, or code execution paths. Files modified are static markdown/docs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T (Tampering) | README.md | mitigate | Verify with grep that all Courtsite/sinar references are removed after edit (`rg -i "sinar|courtsite" README.md` returns empty) |
| T-08-02 | T (Tampering) | STRUCTURE.md | mitigate | Verify with grep that no sinar/scripts entries remain after edit |
| T-08-03 | R (Repudiation) | Both files | accept | Documentation changes are fully tracked in git history; any stale content can be traced and reverted |
</threat_model>

<verification>
1. Run `rg -i "sinar|courtsite|enjin|Courtsite|COURTSITE_DIR" README.md` — must return zero matches
2. Run `rg -i "sinar|courtsite|enjin|update-migration-pr" .planning/codebase/STRUCTURE.md` — must return zero matches
3. Run `grep -c "git@github.com:uz6r" README.md` — must return >= 1 (URL updated)
4. Run `grep -c "audit-nvim-plugins" README.md` — must return >= 1 (directory tree updated)
5. Verify both files still parse and render correctly (no broken markdown)
</verification>

<success_criteria>
- README.md has zero Courtsite/sinar/enjin references
- README.md shows correct git clone URL (`uz6r`)
- README.md directory tree shows `audit-nvim-plugins` and `profile-zsh` under `bin/`
- README.md no longer has the "Courtsite-related aliases" note
- STRUCTURE.md directory tree shows current bin/ contents and no scripts/ entry
- Both files are valid markdown with no broken syntax
</success_criteria>

<output>
After completion, create `.planning/phases/08-verify-update-documentation/08-01-SUMMARY.md`
</output>
