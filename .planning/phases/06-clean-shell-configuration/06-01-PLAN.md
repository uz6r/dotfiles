---
phase: 06-clean-shell-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [zsh/.zshrc, scripts/update-migration-pr]
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-03, SHELL-04]
must_haves:
  truths:
    - "User can start a new zsh session without errors (`zsh -n .zshrc` passes)"
    - "User can use shell without `localdev()` function available (function removed)"
    - "User can view .zshrc with no commented Courtsite aliases (lines 311-328 removed entirely)"
    - "User can verify cross-platform detection works (`is_darwin()`, `is_linux()` functions present and functional)"
  artifacts:
    - path: "zsh/.zshrc"
      provides: "Clean shell config with no Courtsite references"
    - path: "scripts/update-migration-pr"
      provides: "DELETED (Courtsite-specific script removed)"
  key_links:
    - from: "zsh/.zshrc"
      to: "cross-platform detection"
      via: "is_darwin() and is_linux() functions"
      pattern: "is_darwin\\(\\)|is_linux\\()"
---

<objective>
Clean .zshrc from all Courtsite references and delete Courtsite-specific script.

Purpose: Remove company-specific configuration since leaving Courtsite, while preserving cross-platform detection logic.
Output: Clean .zshrc with zero Courtsite references, scripts/update-migration-pr deleted.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-clean-shell-configuration/06-CONTEXT.md
@zsh/.zshrc
@scripts/update-migration-pr
</context>

<tasks>

<task type="auto">
  <name>task 1: Remove all Courtsite references from .zshrc</name>
  <files>zsh/.zshrc</files>
  <action>Remove the following Courtsite-specific sections from zsh/.zshrc per D-01, D-02, D-03, D-07, D-08:
  
  1. Remove lines 153-184 (localdev() function and start_tmux_layout(), including the TODO comment at line 153)
  2. Remove lines 311-328 (commented Courtsite aliases, TODO markers, and active sinar-pi-setup and sinar-pi-wifi-setup aliases)
  
  PRESERVE the following per D-07 and D-08:
  - Lines 36-77: Cross-platform detection logic (IS_MACOS, IS_LINUX, is_darwin(), is_linux(), case "$(uname -s)")
  - Lines 60-77: Homebrew PATH configuration (within the preserved section above)
  - Line 80: PATH configuration including $HOME/uz6r/dotfiles/scripts (per D-05, keep scripts/ in PATH)
  
  After removal, verify no Courtsite references remain: run `grep -i "localdev\|sinar\|courtsite\|COURTSITE_DIR" zsh/.zshrc` — should return no matches.
  </action>
  <verify>
    <automated>! grep -i "localdev\|sinar\|courtsite\|COURTSITE_DIR" zsh/.zshrc && echo "✅ No Courtsite references found"</automated>
  </verify>
  <done>zsh/.zshrc has zero Courtsite references, cross-platform detection logic preserved (is_darwin(), is_linux() functions present), Homebrew PATH config preserved, scripts/ directory kept in PATH.</done>
</task>

<task type="auto">
  <name>task 2: Delete Courtsite-specific script (update-migration-pr)</name>
  <files>scripts/update-migration-pr</files>
  <action>Delete the scripts/update-migration-pr file per D-04. This script is Courtsite-specific (references Courtsite-Internal/enjin repos and infrastructure). After deletion, the scripts/ directory will be empty but per D-05, the PATH entry in .zshrc (line 80) should remain for future utility scripts.

  Command: `rm scripts/update-migration-pr`
  </action>
  <verify>
    <automated>test ! -f scripts/update-migration-pr && echo "✅ scripts/update-migration-pr deleted"</automated>
  </verify>
  <done>scripts/update-migration-pr file no longer exists in the repository.</done>
</task>

<task type="auto">
  <name>task 3: Verify .zshrc cleanup and syntax</name>
  <files>zsh/.zshrc</files>
  <action>Verify the .zshrc cleanup is complete and the file has no syntax errors:
  
  1. Run `zsh -n zsh/.zshrc` to verify syntax (Success Criterion 1)
  2. Verify `localdev()` function is not available: `grep -q "localdev()" zsh/.zshrc` should fail (Success Criterion 2)
  3. Verify no commented Courtsite aliases remain: `grep -i "courtsite\|sinar" zsh/.zshrc` should return no matches (Success Criterion 3)
  4. Verify cross-platform detection works: `grep -E "is_darwin\\(\\)|is_linux\\(\\)" zsh/.zshrc` should return both function definitions (Success Criterion 4)
  </action>
  <verify>
    <automated>zsh -n zsh/.zshrc && echo "✅ zsh syntax OK"</automated>
  </verify>
  <done>zsh/.zshrc passes syntax check, localdev() function removed, no Courtsite aliases present, is_darwin() and is_linux() functions preserved and functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → .zshrc | User-supplied shell configuration, trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T (Tampering) | zsh/.zshrc | mitigate | Verify syntax with `zsh -n` after modifications to prevent shell startup failures |
| T-06-02 | I (Information Disclosure) | zsh/.zshrc | mitigate | Remove all Courtsite references including COURTSITE_DIR variable paths that may reveal directory structure |
</threat_model>

<verification>
- `zsh -n zsh/.zshrc` passes (no syntax errors)
- `grep -i "localdev\|sinar\|courtsite\|COURTSITE_DIR" zsh/.zshrc` returns no matches
- `grep -E "is_darwin\\(\\)|is_linux\\(\\)" zsh/.zshrc` returns both function definitions
- `scripts/update-migration-pr` file does not exist
</verification>

<success_criteria>
- [ ] `zsh -n zsh/.zshrc` passes without errors
- [ ] `localdev()` function not found in .zshrc
- [ ] No commented Courtsite aliases in .zshrc (lines 311-328 removed)
- [ ] `is_darwin()` and `is_linux()` functions present and functional in .zshrc
- [ ] `scripts/update-migration-pr` file deleted
- [ ] Cross-platform detection logic preserved (lines 36-77)
</success_criteria>

<output>
After completion, create `.planning/phases/06-clean-shell-configuration/06-01-SUMMARY.md`
</output>
