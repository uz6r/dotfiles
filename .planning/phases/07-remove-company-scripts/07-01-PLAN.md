# Plan 07-01: Remove Courtsite Scripts from bin/ & Verify Stow

**Phase:** 7 (Remove Company Scripts)
**Wave:** 1 (only wave — all work in one phase)
**Depends on:** None (Phase 6 shell cleanup complete; this is the second cleanup phase)
**Autonomous:** true — all verification is CLI-based

## Objective

Remove `bin/sinar-pi-setup` and `bin/sinar-pi-wifi-setup` from the stow package, verify no broken symlinks remain, and update the Courtsite guard to reflect the clean state.

## Requirements Addressed

- SCRIPT-01: Remove sinar-pi-setup and sinar-pi-wifi-setup from bin/
- SCRIPT-02: No broken symlinks after removal
- SCRIPT-03: PATH scripts/ reference cleaned up

## Files Modified

- `bin/sinar-pi-setup` — deleted
- `bin/sinar-pi-wifi-setup` — deleted
- `zsh/.zshrc` line 80 — `$HOME/uz6r/dotfiles/scripts` removed from PATH

## Files Read First

- `bin/sinar-pi-setup` (current state — what's being removed)
- `bin/sinar-pi-wifi-setup` (current state)

## Tasks

### Task 1: Delete sinar-pi-setup from bin/
**read_first:** `bin/sinar-pi-setup`

**action:** `rm bin/sinar-pi-setup`

**acceptance_criteria:** `ls -la bin/sinar-pi-setup 2>/dev/null; echo $?` → `1` (file not found)

---

### Task 2: Delete sinar-pi-wifi-setup from bin/
**read_first:** `bin/sinar-pi-wifi-setup`

**action:** `rm bin/sinar-pi-wifi-setup`

**acceptance_criteria:** `ls -la bin/sinar-pi-wifi-setup 2>/dev/null; echo $?` → `1` (file not found)

---

### Task 3: Verify stow state — no broken symlinks
**read_first:** `Makefile` (to understand existing `make status` and `make clean` patterns)

**action:** Run `stow -D bin && stow bin` — stow -D removes all `bin/` symlinks from `~/bin/`, stow bin re-links (nothing to link since source is empty). Verify exit code 0.

Then run `make status` — confirm `~/bin/` entries are all healthy (no broken symlinks, no missing files).

**acceptance_criteria:**
- `stow -D bin && stow bin; echo $?` → `0` (stow clean state)
- `make status` → no `❌` entries in output (all symlinks healthy)

---

### Task 4: Update PATH — remove scripts/ reference
**read_first:** `zsh/.zshrc` (line 80 context)

**action:** Edit `zsh/.zshrc` line 80 — remove `$HOME/uz6r/dotfiles/scripts` from PATH. The `scripts/` directory is now empty after `update-migration-pr` removal; no path entry needed.

**acceptance_criteria:** `grep '$HOME/uz6r/dotfiles/scripts' zsh/.zshrc` → empty (no match)

---

### Task 5: Update courtsite-guard — remove bin/ exclusion
**read_first:** `Makefile` (line 222 — courtsite-guard target)

**action:** Edit `Makefile` courtsite-guard target (line 222). Remove `-g '!bin'` from the `rg` exclusion list. The `bin/` directory is now clean — no Courtsite scripts remain to exclude from the guard search.

**acceptance_criteria:** `grep "\-g '!bin'" Makefile` → `1` (no longer in guard)

---

### Task 6: Final verification — make test passes
**read_first:** `Makefile` (test target)

**action:** Run `make test` — this runs all dotfiles validation tests including courtsite-guard, zsh syntax check, git config, and nvim config.

**acceptance_criteria:** `make test; echo $?` → `0` (all tests pass)